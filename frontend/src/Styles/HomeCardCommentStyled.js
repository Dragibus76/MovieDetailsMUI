import CardHeader from "@mui/material/CardHeader";
import Typography from "@mui/material/Typography";
import styled from "styled-components";

export const StyledCardHeader = styled(CardHeader)`
  background-color: #fff;
  margin-top: 1rem;
  width: auto;
  border-radius: 8px;
  box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;
  text-align: left;
`;

export const StyledTypographyPseudo = styled(Typography)`
  color: #000;
  font-weight: 800;
  font-size: 1rem;
`;
export const StyledTypographyResponse = styled(Typography)`
  color: #000;
  font-weight: 800;
  font-size: 1rem;
`;